<UserControl x:Class="GenteFit_WPF.Views.AltaSalaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<StackPanel Margin="20">

		<TextBlock Text="Alta de Sala"
                   FontSize="20"
                   Margin="0 0 0 20" />

		<TextBlock Text="Nombre de la sala"/>
		<TextBox x:Name="NombreTextBox" Width="200"/>

		<TextBlock Text="Aforo máximo" Margin="0 10 0 0"/>
		<TextBox x:Name="AforoTextBox" Width="200"/>

		<CheckBox x:Name="DisponibleCheckBox"
                  Content="Sala disponible"
                  Margin="0 10 0 0"
                  IsChecked="True"/>

		<Button Content="Guardar"
                Width="200"
                Margin="0 20 0 0"
                Click="GuardarSala_Click"/>

	</StackPanel>

</UserControl>

