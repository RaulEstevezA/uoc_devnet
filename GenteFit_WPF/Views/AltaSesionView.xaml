<UserControl x:Class="GenteFit_WPF.Views.AltaSesionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="25" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ALTA DE SESIÓN"
                   FontSize="22"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0 0 0 20"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Top" Width="250">

            <!-- ACTIVIDAD -->
            <TextBlock Text="Actividad" Margin="0 0 0 5"/>
            <ComboBox x:Name="ActividadCombo" Width="250" Height="25"/>

            <!-- SALA -->
            <TextBlock Text="Sala" Margin="0 10 0 5"/>
            <ComboBox x:Name="SalaCombo" Width="250" Height="25"/>

            <!-- MONITOR -->
            <TextBlock Text="Instructor" Margin="0 10 0 5"/>
            <ComboBox x:Name="InstructorCombo" Width="250" Height="25"/>

            <!-- FECHA -->
            <TextBlock Text="Fecha" Margin="0 10 0 5"/>
            <DatePicker x:Name="FechaPicker" Width="250"/>

            <!-- HORA INICIO -->
            <TextBlock Text="Hora inicio" Margin="0 10 0 5"/>
            <ComboBox x:Name="HoraCombo" Width="250" Height="25"/>

            <!-- BOTON GUARDAR -->
            <Button Content="Crear sesión"
                    Width="250"
                    Height="30"
                    Margin="0 20 0 0"
                    Click="CrearSesion_Click"/>
        </StackPanel>
    </Grid>
</UserControl>

